const os = require('os')
console.log(os.platform()) // It's used to give the platform of the system
console.log(os.hostname()) // It's used to give the hostname of the system
console.log(os.cpus()) // It's used to give the information about your system
console.log(process.cwd()) // It's used to give the current file path
console.log(process.pid) // Used to give a process ID